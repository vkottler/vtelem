default: {type: string}
mappings:
  keysrules: {type: integer}
  type: dict
  valuesrules: {regex: '[a-z]+', type: string}
name: {regex: '[a-z_]+', type: string}
type: {regex: ^user_enum$, type: string}
