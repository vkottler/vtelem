default_target: "groups-all"

params:
  project: "vtelem"

includes:
- "config/includes/python.yaml"

configs:
- "local/configs"

variables:
- "local/variables"

templates:
- "local/templates"

compiles:
- name: "local"

renders:
- name: "README.md"
  output_dir: "."
  dependencies:
  - "compiles-local"

groups:
- name: "all"
  dependencies:
  - "renders-README.md"
  - "groups-python"
